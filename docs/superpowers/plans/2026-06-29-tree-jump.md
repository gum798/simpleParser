# 트리 개선(좌측 분할·본문 폰트·클릭 이동) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 트리를 좌측 분할로 보이게 하고, 트리 폰트를 에디터 본문과 같게 하며, 트리 노드 클릭 시 에디터가 해당 소스 범위로 스크롤+선택하게 한다(전체 포맷; JSON/YAML 정확, XML/HTML 근사).

**Architecture:** `tree.ts`의 JSON/YAML 빌더를 파서 AST 기반으로 바꿔 노드별 소스 오프셋(`TreeNode.pos`)을 캡처(JSON=jsonc-parser `parseTree`, YAML=`yaml` AST `.range`; 로그 추출 블록은 절대 오프셋 보정). XML/HTML은 `pos` 없이 빌드하고 클릭 시 순수 함수 `treeJump.approxFind`로 텍스트 검색. `editor.revealRange`가 선택+스크롤. `renderTree(root, onJump)`가 클릭을 위로 전달하고 `ui.ts`가 결선. 레이아웃은 `index.html` 좌우 래퍼 + CSS.

**Tech Stack:** TypeScript, CodeMirror 6(`@codemirror/state` selection/dispatch), jsonc-parser(`parseTree`), yaml(`isMap`/`isSeq`/`isScalar`/`.range`), Vitest(jsdom), Playwright. 추가 런타임 의존성 없음.

## Global Constraints

- 추가 런타임 의존성 없음 — 기존 `jsonc-parser`/`yaml`/CodeMirror만 사용.
- `TreeNode.pos`는 `{ from: number; to: number }` 소스 오프셋(JSON/YAML만 채움; XML/HTML/Markdown은 없음).
- `editor.revealRange(from, to)`는 `from`/`to`를 `[0, doc.length]`로 클램프(편집 후 오프셋이 어긋나도 크래시 없음).
- 클릭 이동 = 스크롤 + 그 범위 **선택**(`selection {anchor, head}` + `scrollIntoView` + `focus`).
- JSON 정확 이동은 단일 문서/관용 복구/로그 추출 모두 지원(추출은 블록 시작 오프셋으로 보정).
- 트리를 열면 **좌우 분할**(왼쪽 트리 ≈34%·오른쪽 에디터), 닫으면 에디터 전체폭. 마크다운 미리보기도 같은 좌측 패널.
- 트리 폰트 크기·계열 = 에디터 본문(`.cm-content`).
- 기존 기능(정렬/트리/미리보기/저장/하이라이트/JSON 추출) 회귀 없음. 단일 `dist/index.html` 유지.
- tsconfig strict(`verbatimModuleSyntax`/`noUnusedLocals`) — 타입 전용 import는 `import type`.
- 커밋 메시지 끝 트레일러: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure

| 파일 | 변경 |
|---|---|
| `src/editor.ts` | `Editor.revealRange(from,to)` 추가; 순수 `clampRange(from,to,len)` 추가 |
| `src/treeJump.ts`(신규) | 순수 `approxFind(text, node): {from,to}\|null` (XML/HTML 노드 토큰 검색) |
| `src/types.ts` | `TreeNode.pos?: { from:number; to:number }` 추가 |
| `src/format/json.ts` | `topLevelSpans` export; `extractJsonSpans(text): {text,start}[]` 추가(블록+절대오프셋); `extractJsonBlocks`는 그걸 위임 |
| `src/tree.ts` | `jsonTree`를 `parseTree` 기반(`jsoncToTree`)으로, `yamlTree`를 AST 기반(`yamlNodeToTree`)으로 — `pos` 캡처. `renderTree(root, onJump)`로 클릭 콜백 |
| `src/ui.ts` | `renderTree(root, onJump)` 결선 — `jumpTo(node)`: `pos`면 `revealRange`, 아니면 `approxFind`→`revealRange` |
| `index.html` | `#editor`+`#panel`을 `<div id="content">` 좌우 래퍼로 감쌈 |
| `src/styles.css` | `#content` flex row, `#panel` 좌측 열, `.tree` 폰트=본문, 라벨 `cursor:pointer` |
| `test/*` | `editor`/`treeJump`/`tree` 유닛 보강·신규, `e2e.spec.ts` 보강 |

---

## Task 1: editor.revealRange + clampRange

**Files:**
- Modify: `src/editor.ts`
- Test: `test/editor.test.ts` (보강)

**Interfaces:**
- Consumes: 기존 `createEditor`/CodeMirror `EditorView`.
- Produces: `clampRange(from:number, to:number, len:number): {from:number; to:number}` (export, 순수); `Editor.revealRange(from:number, to:number): void`.

- [ ] **Step 1: 실패 테스트 작성**

`test/editor.test.ts` 끝에 추가(파일 상단 import에 `clampRange` 추가):
```ts
import { toCmDiagnostics, clampRange } from '../src/editor';
```
```ts
test('clampRange: 범위를 [0, len]으로 클램프하고 from<=to 보장', () => {
  expect(clampRange(2, 5, 10)).toEqual({ from: 2, to: 5 });
  expect(clampRange(-3, 100, 10)).toEqual({ from: 0, to: 10 });
  expect(clampRange(8, 4, 10)).toEqual({ from: 4, to: 8 }); // 뒤집힌 입력 정렬
});
```
(기존 `import { toCmDiagnostics } from '../src/editor';` 줄을 위 줄로 교체)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run test/editor.test.ts`
Expected: FAIL (`clampRange` is not exported).

- [ ] **Step 3: 구현**

`src/editor.ts`의 `toCmDiagnostics` 위 또는 아래에 추가:
```ts
export function clampRange(from: number, to: number, len: number): { from: number; to: number } {
  const a = Math.max(0, Math.min(from, len));
  const b = Math.max(0, Math.min(to, len));
  return { from: Math.min(a, b), to: Math.max(a, b) };
}
```

`Editor` 인터페이스(메서드 목록)에 한 줄 추가:
```ts
  revealRange(from: number, to: number): void;
```

`createEditor`의 반환 객체에 추가(`view`를 캡처):
```ts
    revealRange: (from, to) => {
      const { from: a, to: b } = clampRange(from, to, view.state.doc.length);
      view.dispatch({ selection: { anchor: a, head: b }, scrollIntoView: true });
      view.focus();
    },
```

- [ ] **Step 4: 테스트 통과 + 회귀**

Run: `npx vitest run test/editor.test.ts` → PASS. `npm test` → 전체 PASS. `npx tsc --noEmit` → 클린.

- [ ] **Step 5: 커밋**

```bash
git add src/editor.ts test/editor.test.ts
git commit -m "feat: add editor.revealRange + clampRange

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: treeJump.approxFind (순수)

**Files:**
- Create: `src/treeJump.ts`
- Test: `test/treeJump.test.ts`

**Interfaces:**
- Consumes: `TreeNode` from `src/types.ts` (import type).
- Produces: `approxFind(text:string, node:TreeNode): {from:number; to:number} | null` — 노드의 검색 토큰을 `text`에서 찾아 범위 반환(없으면 null). element→`<tag`, `@attr`→`attr=`, scalar→값. object/array는 null.

- [ ] **Step 1: 실패 테스트 작성**

`test/treeJump.test.ts`:
```ts
import { test, expect } from 'vitest';
import { approxFind } from '../src/treeJump';
import type { TreeNode } from '../src/types';

const xml = '<note id="x"><to>Tove</to></note>';

test('element 노드는 태그 이름 위치를 찾는다(< 제외)', () => {
  const node: TreeNode = { key: 'to', type: 'element' };
  const r = approxFind(xml, node)!;
  expect(xml.slice(r.from, r.to)).toBe('to'); // '<to'에서 이름 'to'만 선택(@attr→이름과 일관)
});

test('scalar 노드는 값 텍스트를 찾는다', () => {
  const node: TreeNode = { type: 'scalar', value: 'Tove' };
  const r = approxFind(xml, node)!;
  expect(xml.slice(r.from, r.to)).toBe('Tove');
});

test('속성 노드(@id)는 attr= 를 찾는다', () => {
  const node: TreeNode = { key: '@id', type: 'scalar', value: 'x' };
  const r = approxFind(xml, node)!;
  expect(xml.slice(r.from, r.to)).toBe('id');
});

test('못 찾으면 null', () => {
  expect(approxFind(xml, { type: 'scalar', value: '없음' })).toBeNull();
  expect(approxFind(xml, { type: 'object' })).toBeNull();
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run test/treeJump.test.ts`
Expected: FAIL (`Cannot find module '../src/treeJump'`).

- [ ] **Step 3: 구현**

`src/treeJump.ts`:
```ts
import type { TreeNode } from './types';

/** XML/HTML 노드의 소스 위치를 텍스트 검색으로 근사한다(정확 오프셋이 없을 때). */
export function approxFind(text: string, node: TreeNode): { from: number; to: number } | null {
  const token = searchToken(node);
  if (token === null) return null;
  const idx = text.indexOf(token);
  if (idx === -1) return null;
  // 속성/태그는 접두 기호를 제외한 이름 부분만 선택
  if (node.type === 'element') return { from: idx + 1, to: idx + token.length }; // '<' 제외
  if (node.key && node.key.startsWith('@')) return { from: idx, to: idx + token.length - 1 }; // '=' 제외
  return { from: idx, to: idx + token.length };
}

function searchToken(node: TreeNode): string | null {
  if (node.type === 'element') return `<${node.key ?? ''}`;
  if (node.key && node.key.startsWith('@')) return `${node.key.slice(1)}=`;
  if (node.type === 'scalar' && node.value) return node.value;
  return null; // object/array 등은 검색 토큰 없음
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run test/treeJump.test.ts` → PASS (4 passed). `npx tsc --noEmit` → 클린.

- [ ] **Step 5: 커밋**

```bash
git add src/treeJump.ts test/treeJump.test.ts
git commit -m "feat: add approxFind for XML/HTML tree-node source search

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: JSON 위치 캡처 트리 (parseTree)

**Files:**
- Modify: `src/types.ts` (`TreeNode.pos`), `src/format/json.ts` (`topLevelSpans` export, `extractJsonSpans`), `src/tree.ts` (`jsonTree` → `jsoncToTree`)
- Test: `test/tree.test.ts` (보강)

**Interfaces:**
- Consumes: jsonc-parser `parseTree`/`Node`; `topLevelSpans`/`extractJsonSpans`/`parseJsonTolerant` from `src/format/json.ts`.
- Produces: `TreeNode.pos?: {from,to}`; `extractJsonSpans(text): {text:string; start:number}[]` (export); `topLevelSpans(text): Array<[number,number]>` (export). JSON 트리 노드가 정확한 `pos`를 가짐.

- [ ] **Step 1: 실패 테스트 작성**

`test/tree.test.ts` 끝에 추가:
```ts
test('JSON 트리 노드는 정확한 소스 pos를 가진다', () => {
  const text = '{"a":1,"b":[2,3]}';
  const r = buildTree(text, 'json');
  const a = r.root?.children?.find((c) => c.key === 'a')!;
  expect(text.slice(a.pos!.from, a.pos!.to)).toBe('1'); // a의 값 위치
  const b = r.root?.children?.find((c) => c.key === 'b')!;
  expect(text.slice(b.pos!.from, b.pos!.to)).toBe('[2,3]'); // b의 배열 위치
});

test('로그 추출 JSON도 절대 오프셋 pos', () => {
  const text = 'log body={"x":1} end';
  const r = buildTree(text, 'json');
  // 단일 블록이면 그 객체가 root
  expect(text.slice(r.root!.pos!.from, r.root!.pos!.to)).toBe('{"x":1}');
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run test/tree.test.ts`
Expected: FAIL (`a.pos` is undefined).

- [ ] **Step 3: types.ts — TreeNode.pos 추가**

`src/types.ts`의 `TreeNode`에 한 줄 추가(`partial?` 다음):
```ts
  pos?: { from: number; to: number };
```

- [ ] **Step 4: format/json.ts — topLevelSpans export + extractJsonSpans**

`topLevelSpans` 선언을 `export function topLevelSpans(...)`로 바꾸고(앞에 `export` 추가), `extractJsonBlocks`를 다음으로 교체:
```ts
/** 블록 텍스트와 그 절대 시작 오프셋을 함께 추출한다(중첩 복구 시 base로 절대 위치 유지). */
export function extractJsonSpans(text: string, base = 0, depth = 0): Array<{ text: string; start: number }> {
  if (depth > 40) return [];
  const out: Array<{ text: string; start: number }> = [];
  for (const [s, e] of topLevelSpans(text)) {
    const span = text.slice(s, e + 1);
    try {
      JSON.parse(span);
      out.push({ text: span, start: base + s });
    } catch {
      out.push(...extractJsonSpans(text.slice(s + 1, e), base + s + 1, depth + 1));
    }
  }
  return out;
}

export function extractJsonBlocks(text: string): string[] {
  return extractJsonSpans(text).map((b) => b.text);
}
```
(이러면 `extractJsonBlocks` 출력은 기존과 동일 — detect.ts/포맷 회귀 없음.)

- [ ] **Step 5: tree.ts — jsonTree를 parseTree 기반으로**

`src/tree.ts` 상단 import 교체:
```ts
import { parseTree, type Node as JsoncNode } from 'jsonc-parser';
import type { Diagnostic, Format, TreeNode, TreeResult } from './types';
import { extractJsonSpans, topLevelSpans, parseJsonTolerant } from './format/json';
import { parseYamlTolerant } from './format/yaml';
import { xmlDiagnostics } from './format/xml';
```
(`resolveJson` import 제거 — 더 이상 사용 안 함.)

`jsonTree` 함수 전체를 교체:
```ts
function jsonTree(text: string): TreeResult {
  const spans = topLevelSpans(text);
  const isWholeSingleSpan =
    spans.length === 1 &&
    text.slice(0, spans[0][0]).trim() === '' &&
    text.slice(spans[0][1] + 1).trim() === '';
  let validWhole = false;
  try {
    JSON.parse(text);
    validWhole = true;
  } catch {
    /* not strictly valid */
  }

  if (validWhole || isWholeSingleSpan) {
    const node = parseTree(text, undefined, { allowTrailingComma: true });
    if (!node) return fromValue(parseJsonTolerant(text));
    const root = jsoncToTree(node, undefined, 0);
    const { diagnostics } = parseJsonTolerant(text);
    if (diagnostics.length > 0) root.partial = true;
    return { root, diagnostics };
  }

  // 로그/텍스트 추출 모드: 블록별 parseTree + 절대 오프셋
  const blocks = extractJsonSpans(text);
  const nodes = blocks
    .map((b) => {
      const n = parseTree(b.text, undefined, { allowTrailingComma: true });
      return n ? jsoncToTree(n, undefined, b.start) : null;
    })
    .filter((n): n is TreeNode => n !== null);
  if (nodes.length === 0) return fromValue(parseJsonTolerant(text));
  const root = nodes.length === 1 ? nodes[0] : { type: 'array' as const, children: nodes };
  return { root, diagnostics: [] };
}

function jsoncToTree(node: JsoncNode, key: string | undefined, base: number): TreeNode {
  const pos = { from: base + node.offset, to: base + node.offset + node.length };
  if (node.type === 'object') {
    const children = (node.children ?? []).map((prop) => {
      const k = String(prop.children?.[0]?.value ?? '');
      const valNode = prop.children?.[1];
      if (valNode) return jsoncToTree(valNode, k, base);
      return {
        key: k,
        type: 'scalar' as const,
        value: '',
        pos: { from: base + prop.offset, to: base + prop.offset + prop.length },
      };
    });
    return { key, type: 'object', pos, children };
  }
  if (node.type === 'array') {
    return {
      key,
      type: 'array',
      pos,
      children: (node.children ?? []).map((c, i) => jsoncToTree(c, String(i), base)),
    };
  }
  return { key, type: 'scalar', value: node.type === 'null' ? 'null' : String(node.value), pos };
}
```
(`fromValue`/`valueToNode`는 그대로 둔다 — YAML 폴백·JSON tolerant 폴백에서 사용.)

- [ ] **Step 6: 테스트 통과 + 회귀**

Run: `npx vitest run test/tree.test.ts` → 신규 + 기존 JSON 트리 테스트 PASS.
Run: `npm test` → 전체 PASS(detect/format-json 포함 회귀 없음). `npx tsc --noEmit` → 클린.

- [ ] **Step 7: 커밋**

```bash
git add src/types.ts src/format/json.ts src/tree.ts test/tree.test.ts
git commit -m "feat: capture exact source pos for JSON tree nodes (parseTree)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: YAML 위치 캡처 트리 (AST .range)

**Files:**
- Modify: `src/tree.ts` (`yamlTree` 추가, buildTree의 yaml 케이스 교체)
- Test: `test/tree.test.ts` (보강)

**Interfaces:**
- Consumes: `yaml`의 `parseDocument`/`isMap`/`isSeq`/`isScalar`; `parseYamlTolerant` from `src/format/yaml.ts` (진단용).
- Produces: YAML 트리 노드가 정확한 `pos`를 가짐(`.range`).

- [ ] **Step 1: 실패 테스트 작성**

`test/tree.test.ts` 끝에 추가:
```ts
test('YAML 트리 노드는 정확한 소스 pos를 가진다', () => {
  const text = 'a: 1\nb: two';
  const r = buildTree(text, 'yaml');
  const b = r.root?.children?.find((c) => c.key === 'b')!;
  expect(text.slice(b.pos!.from, b.pos!.to)).toBe('two');
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run test/tree.test.ts`
Expected: FAIL (`b.pos` undefined — yaml 트리가 아직 pos 없음).

- [ ] **Step 3: 구현**

`src/tree.ts` 상단 import에 yaml 추가:
```ts
import { parseDocument, isMap, isSeq, isScalar } from 'yaml';
```

`buildTree`의 yaml 케이스 교체:
```ts
    case 'yaml':
      return yamlTree(text);
```

`jsonTree` 아래에 추가:
```ts
function yamlTree(text: string): TreeResult {
  const { diagnostics } = parseYamlTolerant(text);
  const doc = parseDocument(text, { prettyErrors: true });
  if (doc.contents == null) return { diagnostics };
  const root = yamlNodeToTree(doc.contents, undefined);
  if (diagnostics.length > 0) root.partial = true;
  return { root, diagnostics };
}

function yamlNodeToTree(node: unknown, key: string | undefined): TreeNode {
  const range = (node as { range?: [number, number, number] | null }).range ?? undefined;
  const pos = range ? { from: range[0], to: range[1] } : undefined;
  if (isMap(node)) {
    return {
      key,
      type: 'object',
      pos,
      children: node.items.map((pair) => {
        const k = isScalar(pair.key) ? String(pair.key.value) : String(pair.key);
        return yamlNodeToTree(pair.value, k);
      }),
    };
  }
  if (isSeq(node)) {
    return { key, type: 'array', pos, children: node.items.map((it, i) => yamlNodeToTree(it, String(i))) };
  }
  if (isScalar(node)) {
    return { key, type: 'scalar', value: node.value === null ? 'null' : String(node.value), pos };
  }
  return { key, type: 'scalar', value: node == null ? 'null' : String(node), pos };
}
```

- [ ] **Step 4: 테스트 통과 + 회귀**

Run: `npx vitest run test/tree.test.ts` → 신규 + 기존 YAML 트리 테스트(`YAML 맵 → object 노드`) PASS.
Run: `npm test` → 전체 PASS. `npx tsc --noEmit` → 클린.

- [ ] **Step 5: 커밋**

```bash
git add src/tree.ts test/tree.test.ts
git commit -m "feat: capture exact source pos for YAML tree nodes (AST range)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: renderTree 클릭 콜백 + ui 결선

**Files:**
- Modify: `src/tree.ts` (`renderTree` 시그니처), `src/ui.ts` (jumpTo 결선)
- Test: `test/tree.test.ts` (renderTree onJump)

**Interfaces:**
- Consumes: `editor.revealRange`(T1), `approxFind`(T2), `TreeNode.pos`(T3).
- Produces: `renderTree(root:TreeNode, onJump:(node:TreeNode)=>void): HTMLElement` — 노드 라벨 클릭 시 `onJump(node)`.

- [ ] **Step 1: 실패 테스트 작성**

`test/tree.test.ts`의 기존 `renderTree는 접이식 DOM을 만든다` 테스트에서 `renderTree(r.root!)` 호출이 인자 부족이 되므로, 그 호출을 `renderTree(r.root!, () => {})`로 바꾸고, 아래 테스트를 추가:
```ts
test('트리 라벨 클릭 시 onJump(node) 호출', () => {
  const r = buildTree('{"a":1}', 'json');
  const calls: string[] = [];
  const el = renderTree(r.root!, (n) => calls.push(n.key ?? n.type));
  (el.querySelector('.tree-label') as HTMLElement).click();
  expect(calls.length).toBeGreaterThan(0);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run test/tree.test.ts`
Expected: FAIL (renderTree는 아직 onJump 인자 없음 / 클릭 핸들러 없음).

- [ ] **Step 3: tree.ts — renderTree/renderNode에 onJump 전달**

`renderTree`와 `renderNode`를 교체:
```ts
export function renderTree(root: TreeNode, onJump: (node: TreeNode) => void): HTMLElement {
  const container = document.createElement('div');
  container.className = 'tree';
  container.appendChild(renderNode(root, onJump));
  return container;
}

function renderNode(node: TreeNode, onJump: (node: TreeNode) => void): HTMLElement {
  const el = document.createElement('div');
  el.className = 'tree-node' + (node.partial ? ' partial' : '');
  const keyPart = node.key !== undefined ? `${node.key}: ` : '';
  const label = document.createElement('span');
  label.className = 'tree-label';
  label.addEventListener('click', () => onJump(node));

  if (node.children && node.children.length) {
    const toggle = document.createElement('button');
    toggle.className = 'tree-toggle';
    toggle.textContent = '▾';
    const childrenEl = document.createElement('div');
    childrenEl.className = 'tree-children';
    node.children.forEach((c) => childrenEl.appendChild(renderNode(c, onJump)));
    toggle.addEventListener('click', () => {
      const hidden = childrenEl.style.display === 'none';
      childrenEl.style.display = hidden ? '' : 'none';
      toggle.textContent = hidden ? '▾' : '▸';
    });
    label.textContent = `${keyPart}${typeLabel(node)}`;
    el.append(toggle, label, childrenEl);
  } else {
    label.textContent = `${keyPart}${node.value ?? typeLabel(node)}`;
    el.append(label);
  }
  return el;
}
```

- [ ] **Step 4: ui.ts — jumpTo 결선**

`src/ui.ts` 상단 import에 추가:
```ts
import { approxFind } from './treeJump';
import type { TreeNode } from './types';
```
(기존 `import type { Diagnostic, Format } from './types';` 를 `import type { Diagnostic, Format, TreeNode } from './types';` 로 합쳐도 됨.)

`viewBtn` 클릭 핸들러의 트리 렌더 부분에서 `renderTree(treeRoot)` 호출을 `renderTree(treeRoot, jumpTo)`로 바꾸고, `mountApp` 내부에 `jumpTo`를 추가:
```ts
  function jumpTo(node: TreeNode): void {
    const r = node.pos ?? approxFind(editor.getValue(), node);
    if (r) editor.revealRange(r.from, r.to);
  }
```
(주의: 기존 `if (treeRoot) root.panel.appendChild(renderTree(treeRoot));` → `if (treeRoot) root.panel.appendChild(renderTree(treeRoot, jumpTo));`)

- [ ] **Step 5: 테스트 통과 + 회귀 + 빌드**

Run: `npx vitest run test/tree.test.ts` → PASS. `npm test` → 전체 PASS. `npx tsc --noEmit` → 클린. `npm run build && ls dist` → 단일 `dist/index.html`.

- [ ] **Step 6: 커밋**

```bash
git add src/tree.ts src/ui.ts test/tree.test.ts
git commit -m "feat: wire tree-node click to jump (revealRange/approxFind)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 좌우 분할 레이아웃 + 본문 폰트

**Files:**
- Modify: `index.html`, `src/styles.css`
- Test: 빌드 + (시각) — 자동 검증은 Task 7 e2e

**Interfaces:**
- Consumes: 기존 DOM(`#editor`, `#panel`).
- Produces: 트리 열림 시 좌우 분할; 트리 폰트=본문.

- [ ] **Step 1: index.html — 좌우 래퍼**

`<main id="editor"></main>` 와 `<aside id="panel" hidden></aside>` 를 `<div id="content">` 로 감싼다:
```html
    <div id="content">
      <main id="editor"></main>
      <aside id="panel" hidden></aside>
    </div>
    <aside id="rules" hidden></aside>
```
(`#rules`는 래퍼 밖, 기존 위치 유지.)

- [ ] **Step 2: styles.css — 분할 + 폰트**

`src/styles.css`에 추가/수정:
```css
#content { flex: 1; display: flex; min-height: 0; }
#editor { flex: 1; overflow: auto; min-width: 0; }
#panel { order: -1; width: 34%; max-width: 480px; max-height: none; overflow: auto;
  border-top: none; border-right: 1px solid #8884; }
.tree { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 13px; line-height: 1.5; }
.tree-label { cursor: pointer; }
.tree-label:hover { background: #8882; }
```
(기존 `#panel { ... border-top ... }` 규칙은 위 값으로 갱신; `#editor { flex:1; overflow:auto }` 는 위로 대체. 마크다운 미리보기도 같은 `#panel` 좌측 열에 표시된다.)

- [ ] **Step 3: 빌드 확인**

Run: `npm test` → 회귀 없음. `npx tsc --noEmit` → 클린. `npm run build && ls dist` → 단일 `dist/index.html`.

- [ ] **Step 4: 커밋**

```bash
git add index.html src/styles.css
git commit -m "feat: left-split tree panel and body-sized tree font

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: E2E

**Files:**
- Modify: `test/e2e.spec.ts`

**Interfaces:**
- Consumes: 빌드된 앱.
- Produces: (검증 전용)

- [ ] **Step 1: 테스트 추가**

`test/e2e.spec.ts` 끝에 추가:
```ts
test('트리 노드 클릭 → 에디터가 해당 위치를 선택', async ({ page }) => {
  await page.goto('/');
  await page.locator('.cm-content').click();
  await page.keyboard.type('{"alpha":111,"beta":222}');
  await page.getByRole('button', { name: '정렬' }).click();
  await page.getByRole('button', { name: '트리' }).click();

  // 좌우 분할: 트리 패널이 보임
  await expect(page.locator('#panel .tree-node').first()).toBeVisible();

  // 'beta' 노드 라벨 클릭 → 에디터에 선택 영역 생성
  await page.locator('#panel .tree-label', { hasText: 'beta' }).first().click();
  await expect(page.locator('.cm-selectionBackground').first()).toBeVisible();
});
```

- [ ] **Step 2: e2e 실행**

Run: `npm run e2e`
Expected: 기존 + 신규 전부 PASS. (CodeMirror 선택/스크롤은 실제 브라우저에서 동작. 셀렉터/타이밍이 필요하면 조정하되 단언을 약화하지 말 것.)

- [ ] **Step 3: 커밋**

```bash
git add test/e2e.spec.ts
git commit -m "test: e2e for tree-node click-to-select

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (작성자 점검)

**1. 스펙 커버리지:** §1 레이아웃→T6; 폰트→T6; 클릭이동→T1(revealRange)+T2(approxFind)+T3/T4(pos)+T5(결선)+T7(e2e). §2 위치추적(JSON parseTree·추출 오프셋 보정, YAML range, XML/HTML 근사)→T3/T4/T2. §3 TreeNode.pos→T3. §5 데이터흐름→T5. §6 에러(클램프·null·partial)→T1/T2/T3/T4. §7 테스트→각 T. §8 비범위 준수. 누락 없음.

**2. Placeholder 스캔:** "TBD/이후구현" 없음. 모든 코드 단계에 실제 코드 포함.

**3. 타입 일관성:** `TreeNode.pos`(T3) ↔ jsoncToTree/yamlNodeToTree(T3/T4)가 채우고 ui.jumpTo(T5)가 읽음. `renderTree(root,onJump)`(T5) ↔ ui 호출(T5). `editor.revealRange`(T1) ↔ ui.jumpTo(T5). `approxFind(text,node)`(T2) ↔ ui.jumpTo(T5). `extractJsonSpans`/`topLevelSpans`(T3) ↔ jsonTree(T3). 기존 `extractJsonBlocks` 출력 불변(detect 회귀 없음). 일관됨.
